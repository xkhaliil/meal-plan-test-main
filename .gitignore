# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env — never commit secrets (use `.env.example` for keys list)
.env
.env.local
.env.*.local

# Interviewer-only spec (keep a copy outside the repo if you use it).
breakingPlan.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/app/generated/prisma

# sqlite database
*.db
*.db-journal

# Optional enormous cooking GIFs (~20MB each) for NEXT_PUBLIC_COOKING_GIF_USE_LOCAL_MEGA
public/cooking-gifs/*.gif

.env
